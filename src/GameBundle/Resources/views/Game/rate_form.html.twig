{{ form_start(form, { method: 'post', action: path('new_rate', {'title': game.title}) }) }}
    
    <fieldset>
        <legend>{{ 'title.add_vote'|trans }}</legend>

        {# Render any global form error (e.g. when a constraint on a public getter method failed) #}
        {{ form_errors(form) }}

        <div>
            {{ form_label(form.rate, 'label.rate') }}

            {# Render any errors for the "content" field (e.g. when a class property constraint failed) #}
            {{ form_errors(form.rate) }}

            {{ form_widget(form.rate, { attr: { rows: 10 } }) }}
        </div>

        <div>
            <button type="submit">{{ 'action.add_vote'|trans }}</button>
        </div>
    </fieldset>
{{ form_end(form) }}
